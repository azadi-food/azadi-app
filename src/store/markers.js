const initialState = {
  locations: [],
  filteredLocations: [],
  selectedLocation:{},
};

// export default (state = initialState, action) => {
//   const {type, payload} = action;
//   // :
//   switch(type) {
//     case 'GET-LOCATIONS':
//   };
// }
