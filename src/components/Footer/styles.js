import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  footer: {
    textAlign: 'center',
  },
  contact: {
  },
  social: {
  },
  copyright: {
  },
}));


